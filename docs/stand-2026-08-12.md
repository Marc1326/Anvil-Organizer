# Stand 12.08.2026 — wo wir stehen, was noch fehlt

## In einem Satz

Der Presets-Bereich ist fertig und gepusht. Von den 6 Punkten des Loops sind
**2 gebaut** (1 und 4), **3 offen** (2, 3, 5), **1 gestrichen** (6). OverlayFS
liegt fertig auf einem Branch und wartet auf den Rebase. **Nichts davon ist am
laufenden Anvil oder im Spiel getestet.**

---

## FERTIG UND GEPUSHT

### Presets-Bereich (`6def2a8`, Wiki `ad5f93b`)

Charakter-Presets stehen nicht mehr in der Mod-Liste, sondern in einem eigenen
Bereich darunter — Bauart wie der Frameworks-Bereich. Überschrift bei
Cyberpunk: **„ACU-Presets"**. Spiele ohne Presets zeigen den Bereich nicht.

Erkennung über den **Inhalt**, nicht über den Trenner: alle Dateien müssen die
Preset-Endung tragen und unter dem ACU-Zielpfad liegen. An den echten 536 Mods
gemessen — 7 Presets wandern, `FemV - RE9 Grace` und `Bad Corpo` bleiben in der
Mod-Liste. Genau richtig.

Presets bleiben in der `modlist.txt` (Marcs Entscheidung, Variante 1), sonst
würden sie nie ausgerollt.

**Zwei Fehler nebenbei gefunden und behoben:**

- Beim Umsortieren per Drag & Drop warf Anvil alles weg, was gerade nicht
  angezeigt wird. Presets wären beim ersten Verschieben aus der `modlist.txt`
  geflogen — und nie wieder im Spiel gelandet.
- `tr("dialog.warning")` wurde an drei Stellen benutzt und fehlte in **allen**
  Sprachen. Dort stand der rohe Schlüssel in der Oberfläche.

Der Loop lief über **3 Runden mit 7 Prüfern**, 13 + 12 Befunde, alle behoben.

---

## GEBAUT, NOCH NICHT COMMITTET

### Punkt 1 — Cyberpunk-Ladereihenfolge

**Das war der eigentliche Grund für den ganzen Loop.** Bewiesen in drei
Spielstarts: REDengine liest `archive/pc/mod/modlist.txt` **nicht**, es
entscheidet der Dateiname, und das alphabetisch erste Archiv gewinnt. Marcs
eingestellte Reihenfolge kam im Spiel nie an.

Anvil nummeriert die Archive jetzt beim Ausrollen durch — **umgekehrt zu
Unreal**, wo die letzte Datei gewinnt:

```
000_00_FacePreset_RE9Grace_xBaebsae.archive   höchste Priorität
001_###-PreemFixes-Cloth.archive
...
337_NakedNPCs-8ug8ear-Topless.archive          niedrigste
```

Trocken an den echten Daten gerechnet: **323 Archive** bekommen einen Zähler.
Unberührt bleiben die `.xl`, die REDmod-Archive und alles außerhalb von
`archive/pc/mod`.

Zwei Sachen kamen dabei heraus:

- **Die alte Annahme „85 gekoppelte `.xl` müssen mitbenannt werden" ist falsch.**
  Gemessen: nur 47 von 85 `.xl` heißen wie ihr Archiv, 38 nicht — und die
  funktionieren bei Marc. ArchiveXL verlangt keine Namensgleichheit. Die `.xl`
  bleiben in Ruhe.
- Zwei Mods liefern dieselbe Datei (`NakedNPCs-8ug8ear-Topless.archive`).
  Vorher überschrieb die stärkere die schwächere. Mit Zähler hießen sie
  unterschiedlich und lägen beide da. Anvil räumt die schwächere jetzt weg,
  erkennbar am mitgeschriebenen Originalnamen im Manifest.

### Punkt 4 — Presets umbenennen

Geht wieder, über das Kontextmenü im Presets-Bereich. Eigener Wortlaut
(`context.rename_preset`) in allen 7 Sprachen.

**Stand:** 672 Tests grün, 1 übersprungen.

---

## NOCH ZU BAUEN

### Punkt 2 — Konflikte *innerhalb* von Archiven anzeigen

Marcs eigener Vorschlag. Hätte den Grace-Fall sofort sichtbar gemacht: Anvil
zeigt heute nur Konflikte zwischen losen Dateien, nicht zwischen
Archiv-Inhalten. Das Werkzeug existiert schon
(`anvil/core/redengine_archive.py`, liest 374 Archive in 0,1 s) — es hängt nur
nicht an der Konfliktanzeige.

### Punkt 3 — Verirrte Presets erkennen

Der Fall `Bad Corpo`. Sein Ordner enthält:

```
Bad Corpo/
  Bad Corpo.preset      <- liegt in der WURZEL
  meta.ini
```

Die Datei landet beim Ausrollen als `<Spielordner>/Bad Corpo.preset` — dort
schaut ACU nie hin. **Das Preset wirkt gerade nicht.** Anvil soll solche
verirrten Presets erkennen, melden und geraderücken können.

### Punkt 5 — Der Presets-Trenner

Er heißt „Presets", enthält aber `Bad Corpo`, `Fiore` und alle 11 Frameworks —
weil er der **letzte** Trenner der Liste ist und alles einsammelt, was hinten
angehängt wird. Falsches Schild.

### Punkt 6 — Witcher 3 (GESTRICHEN)

`/mnt/gamingS` gibt es nicht mehr, und Witcher 3 ist auf `/mnt/Gaming` **gar
nicht installiert**. Die Anvil-Instanz zeigt noch auf den toten Pfad. Nichts zu
prüfen, nichts zu testen — zurückgestellt, bis das Spiel wieder da ist.

### OverlayFS

```
Branch:    feat/overlay-deploy  (64f1c47)
Ordner:    /home/mob/Projekte/anvil-overlay
Rückstand: main ist 32 Commits voraus
```

Kein fehlender Code, 298 Tests grün. Zu tun: Rebase auf main, der Blocker mit
den Startoptionen (`WINEDLLOVERRIDES` und Wrapper müssen in **eine** Zeile),
dann ein Durchlauf am echten Spiel. Kein Merge ohne Marcs GO.

---

## WAS MARC MACHEN MUSS

### 1. Der Test — das Wichtigste

Nichts davon hat je im laufenden Anvil gestanden.

```
1. Anvil schließen (läuft als Flatpak)
2. cd "/home/mob/Projekte/Anvil Organizer" && ./restart.sh
3. Unten in der Liste: Bereich "ACU-Presets (7)" — eingeklappt, einmal draufklicken
```

Woran zu erkennen ist, dass es stimmt:

| Wo | Was |
|---|---|
| Neuer Bereich | 7 × `ACU-Preset - …`, Spalte „Variante" zeigt „weiblich" |
| Mod-Liste | `FemV - RE9 Grace` und `Bad Corpo` stehen weiter drin |
| Überschrift | „ACU-Presets" |

### 2. Der Beweis im Spiel

Ein Preset abhaken → Spiel starten → im Charakter-Menü darf es weg sein.

Und für Punkt 1: Nach dem nächsten Spielstart müssten im Spielordner
`archive/pc/mod/` Dateien mit `000_`, `001_` … liegen.

**Achtung, das wird auffallen:** `FemV - RE9 Grace` steht auf Platz 1 der
Liste und bekommt damit `000_` — sie gewinnt gegen `Unique Eyes - Core`
(`008_`), die Augen sind also weg. Das ist **richtig so**: Anvil setzt endlich
die eingestellte Reihenfolge um. Wer die Augen will, schiebt Grace nach unten
oder hakt sie ab — und diesmal wirkt es.

### 3. GitLab

Der Push wurde abgelehnt:

```
HTTP Basic: Access denied ... you're required to use a token instead of a password
```

Zugangsproblem, kein Code-Problem. Nachholen:

```
cd "/home/mob/Projekte/Anvil Organizer" && git push gitlab
```

---

## OFFENE ENTSCHEIDUNGEN

1. **Punkt 1 committen**, bevor 2, 3 und 5 dazukommen? (~180 Zeilen)
2. **`001Bericht/`** liegt untracked im Anvil-Repo, ist aber Fundus-Material
   und die einzige Kopie auf dem Rechner. Gehört nach
   `/home/mob/Projekte/bookmark-extension-analysis/fundus-react/docs/`.
3. **Witcher-3-Instanz** in Anvil: löschen oder auf den toten Pfad stehen lassen?

---

## LEHREN AUS DEM LOOP

- **Meine ersten Tests waren Attrappen.** Zwei Mutationen sind durchgerutscht —
  auch bei meinem ersten Reparaturversuch, weil der Testfall so gebaut war,
  dass der Fehler gar nicht sichtbar werden konnte. Die Presets standen ohnehin
  am Ende der Liste, da fällt Anhängen nicht auf.
- **Eine falsche Diagnose zurückgenommen.** Ein wartender Timer aus meinem Test
  schlug später in einem völlig anderen Test zu. Ich hatte das erst der
  Profilleiste angelastet und dort etwas geändert — falsch, rückgängig gemacht.
- **Eine geerbte Annahme war falsch.** Die „85 gekoppelten `.xl`" standen so im
  Gedächtnis und stimmten nicht. Messen schlägt Erinnern.
- Die qa-pruefer-Agenten haben **kein Write-Tool** — ihre Berichte kommen im
  Antworttext zurück, nicht als Datei.
